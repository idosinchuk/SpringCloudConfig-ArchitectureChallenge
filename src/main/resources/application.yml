# HTTP Server
server:
  port: 8888

spring:
  # Spring Cloud Config Server Repository
  cloud:
    config:
      server:
        git:
          uri: https://github.com/idosinchuk/config-gitRepository-architecture-challenge

  # Spring properties in case you use different profiles
  profiles:
    active: dev
